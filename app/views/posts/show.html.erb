<div>地図</div>
<h1><%= t('@post.title') %></h1>
<div>
  <div>
    <p><%= @post.genre %></p>
    <p><%= @post.region %></p>
    <p><%= @post.prefecture %></p>
  </div>
  <div>
    <p><%= @post.created_at %></p>
  </div>
  <div>
    <p>@<%= @post.account.name %></p>
  </div>
  <div>
    <p>👍<%= @post.favorites.size %></p>
    <p>Like</p>
  </div>
</div>
<% if current_account.id == @post.account.id%>
  <div>
    <%= link_to t('common.destroy'), post_path(@post.id), method: :delete, data: { confirm: t('common.confirm_destroy') } %>
    <%= link_to t('common.edit'), edit_post_path(@post.id), method: :get %>
  </div>
<% end %>